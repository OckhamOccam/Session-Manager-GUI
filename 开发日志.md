# Session-Manager-GUI 开发日志

> `Session-Manager-GUI`（独立插件，当前版本 **0.1.6**）设计与开发记录。
> 相关：`session-editor` 是**另一个独立插件**（不在本仓库内）。
> 日期：2026-09-08 起。
>
> **章节索引**：§0–§20 为 0.1.0 → 0.1.5 的演进与排查记录；
> **§21 为本轮（0.1.6）的缺陷修复、实测证据与经验教训**，新读者可直接跳到该节。

---

## 0. 项目与命名

- 本项目原名 `session-editor-gui`，按需求更名为 **Session-Manager-GUI**：
  目录、包名、loader 行 id、客户端模块 id、API 前缀、自定义事件名、CSS
  作用域 id 等**所有**出现 `session-editor-gui` 的位置全部替换。
- 版本线：首发 **0.1.0**（含多轮修复，见 §9），**0.1.1** 把回收站“彻底删除”
  的确认改为弹出对话框，当前 **0.1.2** 为 DSH 0.1.5-rc.1 适配版。
- 交付目录：`工作区根目录/DSHPlugins/Session-Manager-GUI/`。
- 注意：包名含大写字母，仅适用于本部署的目录拷贝安装；它不是合法 npm
  包名，不能通过 registry 安装（已在手册“已知限制”注明）。

## 1. 需求

初始需求（GUI 化）：
1. 会话行三点菜单追加第 4 项“删除会话”（软删除进回收站）；
2. 设置页新增“会话回收站”，列出被删会话（仅标题），行尾右对齐
   “彻底删除 / 恢复会话”；
3. 其下方新增“已归档会话”，列出已归档会话（仅标题），行尾右对齐
   “删除会话 / 恢复会话”。

本轮（体验精进）需求：
1. “删除会话”菜单项补上图标（与其它三项一致）；
2. 设置里“会话回收站”“已归档会话”要有专属图标（不再是默认齿轮），
   且“会话回收站”与菜单“删除会话”使用**同一个**图标；
3. “归档会话”改名为“**已归档会话**”；
4. 统一用语：界面“删除对话→删除会话”“对话回收站→会话回收站”，插件内
   所有“对话”改为“会话”；
5. 项目更名 Session-Manager-GUI（全部 `session-editor-gui` 引用替换），
   版本号 0.1.0，重写开发日志与使用手册；
6. 存放目录改为 `DSHPlugins/Session-Manager-GUI`。

## 2. 环境

- Harness `0.1.2-rc.1`（原始开发版本）→ 现已升级到 **`0.1.5-rc.1`**（同一
  npx 部署目录原位替换），`$DSH_HOME` 取默认值（即 `~/.dsh`），web profile
  （`@deepseek-ai/dsh-base` + `@deepseek-ai/dsh-web-app`）。
- 关键被补丁产物：
  - `@deepseek-ai/dsh-client-ui-workspace/lib/client.js`
    （SHA-256 `53c40660195c42cde709b802e239f473dd721f45bc329684af31c01fdb73282a`）；
  - `@deepseek-ai/dsh-client-ui-settings-general/lib/client.js`
    （SHA-256 `903bb84407104d5511f38eac13fc740ad147bab6a9c8d51bde52d90cfd996aff`）。

## 3. 调研结论（支撑本轮实现）

1. **会话行菜单**：硬编码在 ui-workspace 产物的 `SessionNodeItem`
   （`sessionMenuItems` 数组 + `onSelect` 分发），无行级 slot；菜单项形状
   `{ id, label, icon? }`，图标写法
   `icon: (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconXxx, {})`
   （16px 图标传 `{}`，20px 图标传 `{ size: 16 }`）。
2. **设置导航图标**：设置壳 `dsh-client-ui-settings-general` 的
   `navIcon(id)` 是硬编码 if 链（models / agent-presets / plugins），
   **未知 id 一律回落到默认齿轮**——因此新增页签想要专属图标，必须补丁
   该产物（没有注册口）。
3. **可用图标清单**（客户端产物中实际出现的名字，去重）：包含
   `IconTrashOutline16`、`IconArchiveOutline20`、`IconEditOutline16`、
   `IconBranchOutline16` 等；`IconTrashOutline16` 被 agent-preset、
   conversation、cordis、goal 等多处使用，安全可用。
4. **客户端模块机制**：`package.json` 的 `dsh.client = { platform: "web" }`
   + `exports["./client"]` 指向 `lib/client.js`（`window.__ModuleLoader__.load`
   格式，factory 内 `require` 仅可用平台 seed/已加载模块；React 走
   `require("react")`）；模块 id = package.json 的 `name`。
5. **主机 JSON 桥**：`ctx.webServer.register({ kind: "prefix", path, handler })`
   （node:http 裸 handler，`ctx.effect` 挂载自动清理）；`/api`、`/plugins`
   为保留前缀。

## 4. 设计决策（含前几轮修复沉淀）

| 决策 | 理由 |
| --- | --- |
| 独立插件、独立版本线（0.1.0） | 与 session-editor 是两个不同插件；不注册同名聊天工具，二者可共存 |
| 单包双半面（主机行 + client 模块） | 一个 loader 行完成挂载，安装面最小 |
| 软删除 = 归档集合隐藏 + 回收站记账 | 产品唯一“隐藏”机制是归档集；这样删除后不会掉进“未分组”，且保留工作区槽位与文件，恢复回原位置 |
| 彻底删除 = 文件立即擦除 + 记账清理；上下文未销毁时留墓碑 | 会话上下文仍挂载时产品目录仍会列出该会话，先隐藏留墓碑、待上下文关闭后由 `sweepPurgedTrash()` 清扫，避免幽灵行 |
| 运行判定 = agents 中 `status === "running"` | sessions 存储保留“本进程打开过”的会话，用它判定会把空闲会话误判为运行中 |
| 标题读取 = 投影缓存 `rows/title.val` 优先 | `sessionQuery.readTitleSnapshots` 对归档会话读取为空；缓存文件是界面同源数据 |
| UI 补丁 = 校验和守护 + 自动备份 + 旧版自动迁移 | 两处原生产物硬编码且无扩展点；升级后重打，脚本可回滚 |
| 菜单“删除会话”与导航“会话回收站”共用垃圾桶图标 | 语义一致、视觉统一（需求 2） |

## 5. 本轮改动明细

| 需求 | 实现 |
| --- | --- |
| 1 菜单图标 | `patch-ui.js` 在插入的第 4 项上加
  `icon: …IconTrashOutline16, {}` |
| 2 设置图标区分 | `patch-ui.js` 新增 settings 目标：`navIcon` 增加
  `session-trash → IconTrashOutline16`、`session-archived → IconArchiveOutline20`
  两个分支；两处补丁共用同一脚本与同一备份机制 |
| 3 改名 | 客户端 zh 字典 `nav/title` 改为“已归档会话”（英文
  “Archived sessions”）；文档同步 |
| 4 统一用语 | 插件与文档全量替换：`对话 → 会话`、`conversation → session`；
  按钮“恢复会话/删除会话/彻底删除”，提示语、错误码文案同步 |
| 5 项目更名 + 0.1.0 | 目录/包名/行 id/模块 id/API 前缀 `/Session-Manager-GUI`/
  事件 `Session-Manager-GUI:delete`/CSS id 全量替换；package.json
  `version: 0.1.0`；重写本文件与使用手册 |
| 6 存放目录 | `DSHPlugins/Session-Manager-GUI/`（旧目录已移除） |

## 6. 验证记录

- `node --check`：主机半面、浏览器半面、补丁脚本全部通过；
- 主机半面冒烟：`apply(ctx)` 注册 1 条 `prefix /Session-Manager-GUI` 路由、
  **0 个工具**（与 session-editor 共存的先决条件）；
- 补丁脚本 `--dry-run`（真实产物）：
  - menu：识别为**旧版补丁 + 可用备份** → 计划“先从备份恢复原版再打新补丁”；
  - settings：识别为原版 → 计划直接打补丁；
  - 两者锚点各命中 1 次，校验和匹配；
- 前几轮已完成并通过的运行时验收（数据层）：软删除→回收站→恢复→
  再次删除→彻底删除；归档→归档页删除→回收站恢复；运行中会话拒绝；
  幽灵行随上下文关闭/重启消失；回收站与归档列表标题正确；
- 本轮图标/改名属 UI 层改动，需部署 + 重启 + 硬刷新后目视确认。

## 7. 限制与风险

1. **两处 UI 补丁耦合版本**：升级 Harness 后需重打；脚本拒绝盲打并给出
   `--dry-run` 指引。
2. **包名大写**：仅支持目录拷贝安装；不能作为 npm 包发布/安装。
3. **菜单文案固定中文**：原生菜单词典不对外暴露注册口。
4. **恢复位置语义**：软删除保留原槽位，恢复回原位置；若为历史遗留条目
   （工作区记账已被移除），恢复会挂到列表顶部。
5. **回收站单进程假定**：`session_trash.json` 无跨实例锁。
6. **自定义路由无鉴权**：`/Session-Manager-GUI` 为同源裸路由，请保持
   Harness 本机/可信网络绑定。
7. **全文检索**：若另行开启持久化检索库，彻底删除不清理其索引行。
8. **历史遗留回收站条目**：0.1.2 之前入回收站的会话其目录仍在 `sessions/`
   树内，插件启动或首次打开回收站时会自动迁移（迁移前它们仍可能被 `@` 命中）。

## 8. 后续路线

- 上游若提供行菜单扩展点与会话删除/恢复 RPC，移除全部 UI 补丁与自建
  API，改用官方通道；
- 设置页图标若上游支持注册，去掉 settings 补丁；
- 回收站批量清空、按工作区筛选、多选彻底删除；
- 菜单项文案国际化（待上游词典机制允许）。

## 9. 变更历史

1. **首版**：设置页两个页签（会话回收站 / 已归档会话）+ 会话行菜单第 4 项 +
   主机回收站 API 与数据层；
2. **修复轮 A**：归档页字段契约（`sessionId`）、标题读取（投影缓存优先）、
   结构化错误码 + 中文提示 + 菜单操作轻提示条；
3. **修复轮 B**：运行判定改为“正在生成/执行”；
4. **修复轮 C**：软删除改为“归档隐藏 + 保留槽位”，杜绝“未分组”；
5. **修复轮 D**：彻底删除的“墓碑 + 延迟清扫”，杜绝幽灵行；
6. **本轮（体验精进）**：菜单图标、设置导航图标（垃圾桶/归档）、
   “已归档会话”改名、用语统一为“会话”、项目更名
   Session-Manager-GUI、版本定为 0.1.0、文档重写；
8. **0.1.2**：适配 DSH 0.1.5-rc.1（补丁校验和/标注）并修复“已删除会话仍能被
   `@` 找到”（软删除改为目录搬迁，见 §13）；
9. **0.1.3**：修复运行时发现的 0.1.5 适配缺陷（`sessionPersistence.list()` 返回形状
   变化导致目录定位失败，见 §14）；
10. **0.1.4**：修复“从回收站恢复后侧栏不显示”（客户端会话目录未重取，见 §16）；
11. **0.1.5**：修复“删除后 `@` 仍能找到”（已挂载会话需 detach，见 §17）。
7. **0.1.1（本轮）**：回收站“彻底删除”由“按钮二次点击确认”改为**弹出
   确认对话框**（文案“是否确认彻底删除？会话彻底删除后不可恢复。”，
   “不可恢复”红色加粗 #FF0000；左“取消”/右“确认彻底删除”；Esc 与
   遮罩点击取消），杜绝误触连击导致不可恢复删除；版本号 0.1.1。

## 10. 部署与验收记录（2026-09-08）

- 部署：新包拷入 `~/.dsh/profiles/node_modules/Session-Manager-GUI/`；补丁行
  由旧 `session-editor-gui` 替换为 `Session-Manager-GUI`（热加载生效：新
  API `/Session-Manager-GUI/...` 正常、旧前缀 404）；旧包目录删除；
  `patch-ui.js` 自动完成会话菜单补丁的“旧版→原版→新版”迁移，并首次打上
  设置导航图标补丁。
- 运行侧核验（重启 + 硬刷新后）：
  - 主机 API：`/Session-Manager-GUI/trash/list`、`/archived/list` 均 200；
  - 补丁产物标记：菜单 `label: "删除会话"`、`IconTrashOutline16`、
    `Session-Manager-GUI:delete` 各就位；设置壳 `session-trash` /
    `session-archived` 两个 navIcon 分支就位；
  - 数据一致性：workspace 成员 ↔ 磁盘目录一致，归档集合与回收站状态自洽；
  - 客户端模块：dsh web 首页与 `/plugins/...` 带认证栅栏，命令行无法直接
    探测；由用户目视确认（菜单项+图标、设置两页签+图标、归档页标题）全部
    正确。
- 验收结论：需求 1–6 全部落地并通过目视与数据层核验。

## 11. 0.1.1 本轮改动（彻底删除确认对话框）

- 需求：回收站“彻底删除”的确认由“同一按钮再次点击”改为弹窗，避免手滑
  连击造成不可挽回的删除；弹窗文案固定，“不可恢复”红色加粗；底部左
  “取消”右“确认彻底删除”。
- 实现（`lib/client.js`）：
  - 新增 `PurgeConfirmDialog` 组件：遮罩 + 面板（`role="dialog"`、
    `aria-modal`），正文由三段 locale 键拼装（`purgeConfirmTitle` +
    红色加粗 `purgeConfirmHighlight` + `purgeConfirmTail`），按钮文案
    `cancel` / `purgeConfirmAction`；
  - 回收站行内按钮只负责 `setPending(entry)` 打开弹窗；确认后执行
    `trash/purge` 并关闭弹窗；失败保留弹窗并在页面显示错误；执行中按钮
    禁用（`busy`）防重复提交；
  - 取消路径：左按钮、Esc、点击遮罩——三者都不会触发删除；
  - 新增 CSS：`.seg-modal-mask/.seg-modal/.seg-modal-text/
    .seg-danger-bold(#FF0000,font-weight:700)/.seg-modal-actions`；
  - 移除旧 `purgeConfirm` 键与 `confirmId` 状态。
- 备份：改动前已将整个项目复制为
  `DSHPlugins/Session-Manager-GUI-0.1.0/`（完整 0.1.0 快照）。
- 未部署：等待下一步指令。

## 12. 适配 DSH 0.1.5-rc.1（0.1.2 版本）

**现象**：DSH 升级后，会话行三点菜单里的“删除会话”消失（设置页的专属
图标也回落到默认齿轮）。

**原因**：DSH 升级在原位（同一 npx 部署目录）替换了被补丁的两个浏览器
产物；补丁内容与同目录 `备份_client.js` 备份一并被清理，因此补丁失效。
插件包（`profiles/node_modules/Session-Manager-GUI`）与补丁行不受影响，
主机半面仍在线。

**适配动作**：
1. 重新比对新版本产物锚点：
   - 会话行菜单：`sessionMenuItems` / `onSelect` 结构与缩进**完全一致**
     （rename/fork/archive 三项、图标组件名、9/5/4/3 制表符层级未变）；
   - 设置壳：`navIcon(id)` 仍是同样的 if 链与未知 id 回落齿轮；
   - 图标可用性：`IconTrashOutline16`、`IconArchiveOutline20` 仍存在。
2. 更新 `install/patch-ui.js` 的两处校验和：
   - `dsh-client-ui-workspace/lib/client.js` →
     `383b9ef779366c13d818500b6488896328b189f156addbaa480c835e902edd5f`
   - `dsh-client-ui-settings-general/lib/client.js` →
     `c5995dba8c3b944a46ebae6bc860b35b8adacd4786105ca636b5b73b84ae396e`
   并把脚本注释中的 Harness 版本标注改为 0.1.5-rc.1。
3. 复核主机半面依赖的契约（均未变，无需改代码）：
   - `workspaceRegistry` 内部形状：`table/global/state/entities/headers/
     sessionPaths/invalidSessionPaths/enqueueOperation/requireState/
     requireTable/setState/rebuildEntities/sessionKnown/readSessionHeader/
     resolveByPath/attachSession` 全部在位；
   - `sessionPersistence.locate`、`sessionQuery.readTitleSnapshots` 在位；
   - `webServer.register({kind,path,handler})` 签名不变；
   - `Agent.status` getter 仍在（`idle`/`running`，由 phase 推导）；
   - 投影缓存布局仍为 `record.rows.title.val`；`workspace.json` 结构不变。
4. 复核客户端侧：`dsh.client` + `exports["./client"]` +
   `window.__ModuleLoader__.load` 机制不变；设置页 `settings.section` 注册
   协议（`id/order/label/locale/inject`）不变；`ctx.effect` /
   `locale.register` / `locale.bind` 仍被新版官方产物使用；扫描器未对包名
   大小写做额外校验（`Session-Manager-GUI` 仍可加载）。

**版本**：本次适配记为 **0.1.2**（文档、package.json、补丁样板同步）。

**待办**：用户将反馈一个隐藏 bug，一并修复后再部署。


## 13. 0.1.2 缺陷修复：已删除会话仍能被 `@` 找到

**现象**：会话经“删除会话”进回收站后，仍能在聊天输入框用 `@` 搜索到并引用。

**根因（源码级）**：
- `@` 提及候选由 `@deepseek-ai/dsh-session-reference` 的
  `SessionReferenceResolver.listCandidates()` 提供；
- 该函数只做三件事：`ctx.sessionQuery.listSessions()` 取会话记录 → 排除自己 →
  按关键词过滤/排序。**既不读 workspace 归档集合，也不认识本插件的回收站记录**；
- 而 0.1.0/0.1.1 的“软删除”仅把会话加入归档集合（隐藏分组面）+ 写回收站条目，
  **日志目录仍留在 `$DSH_HOME/sessions` 内**；
- `sessionQuery.listSessions()` 扫描的正是 `$DSH_HOME/sessions`，于是被删会话照旧
  出现在 `@` 候选里（同理也会出现在任何“按持久化列会话”的面上）。

**修复方案：目录搬迁式软删除（数据层修复，不新增对官方产物的补丁）**
1. `trashAdd`：先把会话目录从 `$DSH_HOME/sessions/<projectKey>/<id>/` **移动**到
   `$DSH_HOME/storages/session_trash/<id>/`——移出持久化扫描范围后，`@`/搜索/列表
   全部看不到；随后仍加入归档集合（覆盖“上下文仍挂载”的情形）。任一步失败都会
   回滚（目录移回 + 归档集合还原），不会出现“文件搬走但无回收站记录”。
2. `trashRestore`：先把目录移回原位（`originalDir` 记在条目里），再执行原有的
   工作区/归档恢复；失败回滚移动。
3. `trashPurge`：relocated 条目直接删除存放区目录；历史条目仍按原位删除。
4. `sweepLegacyRelocation()`：为旧版本遗留（只有归档隐藏、目录未搬迁）的条目补做
   搬迁；触发点 = 插件 `apply()` 启动时一次 + 回收站相关 API 入口；正在生成的
   会话跳过（不搬运行中的目录）。
5. 新增辅助：`trashRoot()`、`sessionsRoot()`、`isInsideSessionsRoot()`（只允许在
   `$DSH_HOME/sessions` 内搬迁）、`sessionDirOf()`、`moveDir()`、`pathExists()`。

**验证（离线，临时 DSH_HOME 真实目录结构 + 假注册表）**：
- A 迁移：遗留条目 → 原目录消失、`storages/session_trash/<id>/` 出现、条目写入
  `relocated/originalDir/storedDir`；
- B 恢复：目录回到原位、回收站条目清空、归档集合移除该 id；
- C 再删除：目录再次移出、归档集合加入该 id；
- D 彻底删除：存放区目录删除、条目清空、工作区记账与归档集合均无残留；
- 语法/导入检查通过；`SUPPORT_NOTE` 与文件头注释已更新为 0.1.5-rc.1。

**未部署**：等待指令；部署后需在 UI 验证“删除后 `@` 搜不到、恢复后又能搜到”。


## 14. 0.1.3 修复：0.1.5 的 `list()` 形状变化导致目录搬迁失效

**发现过程**：0.1.2 部署重启后，用测试会话 一个临时测试会话 做端到端验证。删除接口返回
`{"trashed":true,...,"relocated":false}` 且目录仍留在 `$DSH_HOME/sessions` 内——
说明新代码在跑，但“目录定位”这一步返回了 null。

**根因**：DSH 0.1.5 改了 `sessionPersistence.list()` 的返回形状：
- ≤0.1.2：`SessionHeader[]`（元素本身有 `id`/`cwd`）；
- 0.1.5：`{ header, revision, sizeBytes }[]`（另含 pending 的 `{header, revision}`）。
本插件的 `findHeader()` 仍按“元素即 header”解析，于是永远匹配不到；
`sessionDirOf()` 因此返回 null，`trashAdd` 跳过搬迁（只做了归档隐藏），
`trashRestore`/`trashPurge` 的旧条目分支也同样拿不到目录。

**修复**：
1. `findHeader()` 形状容错：`const header = entry.header ?? entry`；
2. `findHeader()` 对 `list()` 抛错返回 `undefined`（不再把持久化故障放大成 500）；
3. `sessionDirOf()` 改为**文件系统优先**：直接扫描 `$DSH_HOME/sessions/<projectKey>/<id>/`
   （与任何服务返回形状无关），仅在其失败时回退到 `locate(header)`；
4. `trashRestore()` 的 cwd 解析：优先 header.cwd，缺失时回退到回收站条目里
   记录的 `workspacePath`。

**验证（离线，模拟 0.1.5 形状）**：
- A 0.1.5 快照形状下迁移成功（原目录消失、存放区出现、条目写入 relocated/storedDir）；
- B 带 cwd 的恢复成功；
- C header 无 cwd（用 workspacePath 兜底）恢复成功；
- D `list()` 直接抛错时，靠文件系统扫描仍能恢复成功；
- E 彻底删除：存放区目录删除、条目清空、工作区记账清空；
- 部署后另做真实环境核验（见 §15）。

## 15. 0.1.3 部署与真实环境核验记录

- 部署：覆盖 `~/.dsh/profiles/node_modules/Session-Manager-GUI/`（0.1.3）；
  两处 UI 补丁无需重打（0.1.5 校验和已在上一次部署打好）。
- 重启后：插件启动时的一次性 `sweepLegacyRelocation()` 会把 0.1.2 期间入回收站、
  仍未搬迁的条目（如测试会话）自动迁移到存放区。
- 真实环境核验项：删除→目录移出 `sessions/`（`@` 不可见）→ 恢复→目录移回
  （`@` 可见）→ 彻底删除→无残留。


## 16. 0.1.4 修复：从回收站恢复后，侧栏列表不显示该会话

**现象（用户实测）**：从回收站点“恢复会话”后，主机数据全部正确（目录回到
`$DSH_HOME/sessions`、归档集合已移除、工作区槽位保留），但**侧栏会话列表里
看不到它**；同时 `@` 提及能搜到。

**根因**：
- 客户端侧栏的会话目录由客户端 `sessions` 服务（`@deepseek-ai/dsh-api-session-controller`
  的浏览器半面）维护，快照来源是 `manager.refreshList()` → `remote.session.list({})`；
- 该刷新只在**连接代建立时**触发（`handleConnected()` 内部调用 `refreshList()`），
  以及子代理目录等场景；
- 我们的“恢复/彻底删除”只改了主机侧的持久化目录与归档集合，会通过
  `domain/changed` 推送 **归档增量帧**，但**没有任何帧会让客户端重取会话目录**；
- 而 `@` 提及走主机侧的 `sessionReferenceResolver.listCandidates()`
  （`sessionQuery.listSessions()` 实时扫描 `$DSH_HOME/sessions`），所以目录一移回
  就能搜到——两个现象因此并存。

**修复（浏览器半面）**：
- 新增 `refreshSessionCatalog` 钩子（`apply(ctx)` 时安装）：调用客户端
  `ctx.get("sessions")?.refresh?.()`（与重连同一路径），失败仅告警、绝不影响动作本身；
- 在“恢复会话”（回收站页 / 已归档页）、“彻底删除”、以及菜单“删除会话”成功后
  调用 `notifyCatalogChanged()`，让侧栏即时反映主机侧的存在性变化；
- 顺带修掉此前的“彻底删除后目录仍留一行”的观感问题（refresh 会把已删除会话从
  客户端目录中剔除）。

**验证**：`node --check` 通过；部署 + 重启后需在 UI 实测：
从回收站恢复 → 该会话应立刻出现在工作区列表；再次删除 → 立刻消失。


## 17. 0.1.5 修复：删除后 `@` 仍能找到该会话

**现象（用户实测）**：会话经三点菜单删除后进入回收站（目录已确认搬入
`storages/session_trash/<id>/`、归档集合已加入），但聊天框 `@` 仍能搜到它。

**根因**：
- 进程内诊断显示该会话 `store.google` 仍在：`sessions` 存储的 Map 中仍有其
  entry（`entryPresent: true`、`entryHasDetach: true`、`announced: true`），
  `sessions.list()` 仍返回它；
- `sessionQuery` 是 **live-preferred** 服务：`listSessions()` 会把“内存中已挂载
  的会话”与持久化记录合并返回；
- 我们此前的目录搬迁只解决了“持久化侧”，对**仍挂载在内存里**的会话无效——
  `@` 提及候选正是走 `sessionQuery.listSessions()`，因此照样命中。

**修复**：
1. 新增 `detachAttachedSession(ctx, sessionId)`：通过会话存储 entry 自身的
   `detach()` 卸载（产品在“会话上下文结束”时走的同一条路径，会删除 store/
   attachments 中的记录，并在 announced 时广播 `session/disposed`）。形状做了
   特征探测，失败仅告警；
2. `trashAdd`：目录搬迁 + 归档隐藏 + 写条目成功后调用 detach，响应新增
   `detached` 字段；
3. `sweepLegacyRelocation`：迁移旧条目的同时也 detach；
4. `trashPurge`：先尝试 detach；成功则可**立即完成清账**（`final: true`，不再
   需要“墓碑 + 延迟清扫”）；detach 不可用时保留原兜底路径。

**验证（离线，模拟已挂载会话）**：
- A 删除（已挂载）：目录搬入存放区、`detach` 被调用一次、会话不再挂载、
  响应 `{relocated:true, detached:true}`；
- B 彻底删除：`final:true`，存放区目录删除、工作区记账与归档集合清空、
  回收站条目为 0；
- `node --check` 通过。

**部署后需实测**：删除已打开过的会话 → `@` 立即搜不到；恢复后侧栏立即出现、
`@` 可再搜到。

---

## 18. 适配 DSH 0.1.5-rc.2（2026-09-19，版本号仍为 0.1.5）

**现象**：DSH 由 `0.1.5-rc.1` 升级到 `0.1.5-rc.2` 后，会话侧栏行三点菜单里
插件追加的第 4 项“删除会话”又不见了，设置导航的两个页签也回落成默认齿轮。

**排查（先测量再动手）**：

| 检查项 | 手段 | 结论 |
| --- | --- | --- |
| 部署形态 | `readlink -f ~/.dsh/profiles/node_modules/@deepseek-ai/…` | profile 的 `@deepseek-ai/*` 是指向 **npx 部署目录**（`~/.npm/_npx/<hash>/node_modules/…`）的符号链接，升级会重写真实文件 |
| 产物校验和 | `sha256sum` 两处被补丁产物 | `383b9ef7…`（ui-workspace）、`c5995dba…`（ui-settings-general）——**与脚本里 rc.1 的钉值逐字节相同**，即 rc.2 未改这两处产物 |
| 补丁状态 | `grep -c 'Session-Manager-GUI:delete' / 'id === "session-trash"'` | 均为 0；同目录备份 `备份_client.js` 也已随 npm 重装消失 |
| 运行中的真实下发内容 | 拉 `/plugins/events` 的 graph → 取下发给浏览器的 application batch（约 11 MB）→ 检查其中被服务的菜单代码 | 菜单为**未打补丁的三项**（rename / fork / **archive**），`onSelect` 无 `se-delete` 分支；设置 `navIcon` 无 `session-trash` / `session-archived` 分支 |
| 插件本体 | `curl /Session-Manager-GUI/trash/list`、`/archived/list`；客户端 Slot `settings.section` | 主机半面正常（`{"ok":true}`），浏览器半面的两个设置区块仍注册且 `active: true`——**插件本身不需要为 rc.2 改动** |
| 原生菜单 | 反查 rc.2 产物 `SessionNodeItem` | 原生三项为 rename / fork / **archive（归档会话）**，静态数组、无隐藏条件；rc.2 未改 |

**结论**：升级只是把被补丁的两处原生产物换成了未打补丁的原版，并把备份一起清掉；
锚点、图标（`IconTrashOutline16` / `IconArchiveOutline20`，由前端 dist 的 seed
模块 `@deepseek-ai/dsh-client-ui-primitives` 提供）在 rc.2 中依然可用，因此
**重打补丁即可**，无需改锚点、无需改插件代码。

**处理**：

1. `install/patch-ui.js` 头注与钉值来源改记为 `0.1.5-rc.1 / 0.1.5-rc.2`（数值不变）；
2. 重跑补丁 → 两处产物恢复补丁，备份 `备份_client.js` 重新生成；
3. 用 `node --check`（按 ESM 复制成 `.mjs` 校验）确认补丁后产物语法合法；
4. 重新拉取下发 batch 复核：菜单四项（rename / fork / archive / **se-delete**，
   `label: "删除会话"`、垃圾桶图标），`onSelect` 有 `se-delete` 分发，
   `navIcon` 有 `session-trash` / `session-archived` 两个分支。

**同时把补丁脚本升级为“面向未来升级”的形态**（仍不新增版本号）：

| 能力 | 说明 |
| --- | --- |
| `--status` | 打印 realpath、所属包与版本、校验和、是否为已知原版、是否已打补丁、备份是否存在；**已打补丁时用逆变换验证补丁是否还能剥回已知原版**（补丁健康度自检） |
| `--adopt` | 产物不是已知原版且无可用备份时（升级真的改动了产物）：先在内存里试打补丁验证锚点，通过才采纳为**新原版基线**并记入 `install/baselines.json`（含 Harness 版本/时间），随后落盘；同时提示复核上游是否已自带删除/归档入口（那样应撤补丁而非叠加） |
| `--dry-run`（增强） | 未打补丁且产物未知时也**照常验证锚点**并提示“anchors verified — re-run with --adopt”，修掉了“让你去 --dry-run 看锚点、它却拒绝执行”的死循环 |
| `--revert`（增强） | 备份被 npm 升级清掉时，按插入内容做**精确逆变换**剥掉补丁，并把结果与已知原版比对，不一致即拒绝写盘 |

**验证记录（离线自测，全部通过）**：搭了一套 `DSH_HOME=<fixture>` 的假部署
（两处产物的拷贝 + 假 package.json），覆盖：`--status` 原版识别 → `--dry-run`
计划 → 落盘打补丁（生成备份）→ 幂等重跑 → **有备份回滚** → **删备份后逆变换回滚
（结果 sha256 与钉值完全一致）** → 模拟未来版本（改了别处、锚点完好）：默认拒绝、
`--dry-run` 报告锚点通过、`--adopt` 采纳并落盘 → 破坏锚点后连 `--dry-run` 也拒绝。

**运行期发现（值得记入后续文档/流程）**：web profile 的 `client-hmr` 行是
**常挂**的（约 500 ms 轮询每个客户端产物的 mtime/size，变化即 `rebuilt()` 并经
`/plugins/events` 推 SSE）。所以打完补丁**不必重启 `dsh web`**：实测 graph rev
由 `7d29db256299` 变为 `86eef219fa35`，ui-workspace 模块 rev 由
`2ce094d0d0da2f01-28` 变为 `857d5c15617c`，下发 batch 立即变为补丁版；
硬刷新浏览器仍是最稳妥的确认手段。

**遗留**：本次未改版本号（沿用 0.1.5）；`install/baselines.json` 尚未生成
（rc.2 命中内置钉值，无需采纳，该文件仅在 `--adopt` 成功时写入）。

---

## 19. `@` 候选显示裸 UUID 的排查与 `reference` 补丁（2026-09-19）

**用户报告**：升级 rc.2 后，输入框敲 `@` 会拉出一堆“字符串”（形如
`5c882a6f-6c87-4547-9b8e-030ff3ffe10e`），像之前的对话；怀疑插件又出问题。

**排查路径（先证伪“插件造成”）**：

| 检查 | 手段 | 结果 |
| --- | --- | --- |
| 插件是否写过投影缓存 | `grep` 插件主机半面 | 只有 `readFile`（`projectionCacheFile()` 读标题）与 `rm(projectionCacheFile(sessionId))`（**仅清理被彻底删除的那个会话**，line 840）；无任何写入 |
| 插件是否遗漏隐藏 | `/trash/list`、`/archived/list`、`workspace.json` 的 `archivedSessionIds` | 全空；10 个会话目录与 10 个缓存文件一一对应，无幽灵行 |
| 被 @ 的会话是什么 | 会话 id `5c882a6f…` | 2026-09-08 的子代理会话（父会话 `session-4b43999b`＝“DSHSessionManager开发 原型”），**从未被插件删除/恢复过**；它与 `419c080d`、`766ac892` 都不在任何工作区的 `sessionIds` 里（历史子代理遗留） |
| `@` 标签从哪来 | `dsh-session-reference/lib/index.js` `projectedTitle()` | `titleOf(sessionProjectionCache.cachedSnapshot(header, 0, ["title"]))`——**只认当前格式检查点**，否则回退成 `record.header.id` |
| 侧栏为什么有标题 | `dsh-api-session-controller/lib/index.js:1951` | `cachedSnapshot(header, 0) ?? cachedPredecessorTitle(header, 0)`——多一条**前代检查点**回退 |
| 缓存格式差异 | 读 `$DSH_HOME/storages/session_projcache/sessions/*.json` | 5 个 Sep-8 写的文件 `version: 5` 且记录身份**没有 `formatVersion`**；5 个 Sep-10 以后写的 `version: 7` 带 `formatVersion: 3`。域规格当前 `version: 7`（`compatibleVersions: [3,4,5,6]`），文档明确“**没有 `formatVersion` 的记录不能作为折叠捷径**” → `identityMatches` 永不成立 → `cachedSnapshot` 返回 undefined |
| 行版本守卫是否兼容 | title 单元（`dsh-session-title` `key:"title", stateVersion:1`） vs 两份缓存里的 `title.ver` | 新旧都是 `ver: 1` → 前代回退路径的行守卫与 schema 都会通过，回退真能拿到标题 |

**结论**：这是 DSH 上游**两条读取路径不一致**（`@` 严格、侧栏带前代回退），由**升级后投影
缓存域版本提升**触发；插件既没写缓存也没有隐藏遗漏，**不是插件缺陷**。顺带确认另一件事：
“`@` 引用后上下文多出一大段文字”是 `dsh-session-reference` 的**设计行为**（引用即注入
该会话的有界只读快照 + 不可信背景警告，`maxReferences` / `maxReferenceBytes` / spill 可配）。

**处理（用户选择“补丁修复”）**：给 `install/patch-ui.js` 增加第 3 个目标 `reference`
（主机侧，非插件自身功能）：

```js
// 补丁前
return titleOf(this.ctx.get("sessionProjectionCache")?.cachedSnapshot(record.header, SessionLogOffset(0), ["title"]));
// 补丁后
const cache = this.ctx.get("sessionProjectionCache");
return titleOf(cache?.cachedSnapshot(record.header, SessionLogOffset(0), ["title"]) ?? cache?.cachedPredecessorTitle(record.header, SessionLogOffset(0)));
```

- 与侧栏同一套语义：`cachedPredecessorTitle` 只接受**更旧**格式且生命周期身份匹配的记录，
  行版本（`ver === stateVersion`）与 schema 守卫照旧，拿到的是“可能过时但不会错”的标题；
  `isSeeded` 会话仍按设计回退到 id；
- 脚本相应泛化：每个目标自带 `backupSuffix`（主机侧用 `备份_index.js`），`--target` 增加
  `reference`，`all` 覆盖三个目标，收尾提示区分“浏览器侧由 client-hmr 生效”与
  “**主机侧必须重启 `dsh web`**”；
- 钉值：`dsh-session-reference/lib/index.js` = `01c69c1b49d328dccfafbdc1fc88007c42772a43891c21ed500835fd79cc4a56`。

**自测（fixture，全部通过）**：`--status` 识别原版 → `--dry-run` 计划 → 落盘（生成
`备份_index.js`）→ 补丁体缩进/内容正确 → `--status` 自检“可剥回已知原版” → 删备份后
`--revert` 逆变换，**结果 sha256 与钉值完全一致** → `all` 三目标贯通且收尾提示正确。

**待用户验证**：重启 `dsh web` 后敲 `@`，原先 5 条裸 UUID（`419c080d` / `5c882a6f` /
`766ac892` / `session-20201983` / `session-e7c5a4f6`）应显示为标题；`session-fb0682bc`
（另一工作区、标题为 null）仍显示 id，属正常。

---

## 20. “幽灵会话”为何删不掉：子代理可见性 + 旧 id 校验缺陷（2026-09-19）

**用户报告**：`@` 里能看到它们（重启后已显示中文标题），但工作区、回收站、已归档里都没有，
问怎么删掉这些“幽灵”会话。

**排查（三步定位到两件事）**：

1. **它们是谁**：逐会话解压 `session.jsonl.zstd` 读首条记录 →
   `419c080d` / `5c882a6f` / `766ac892` 三条都是
   `{"type":"session","origin":"subagent","parentSession":"session-4b43999b-…",
   "delegationDepth":1,"agentPreset":"cordis","version":0}`——2026-09-08 那次调研跑出来的
   **子代理会话**（父会话＝“DSHSessionManager开发 原型”），其余 5 条都是普通用户会话。
2. **为什么侧栏没有**：产品在 `dsh-client-ui-workspace` 里用一条规则决定行可见性——
   `sessionVisible = origin !== "subagent" && !archived.includes(id) && (!blank || id === current)`。
   **子代理会话永远不进侧栏**（只在父行上聚合为计数徽标）；它们也不在任何工作区的
   `sessionIds` 名下，所以既不在分组里也不在“未分组”里。而 `@` 的候选来自
   `sessionQuery.listSessions()`，只排除自己 → 子代理会话照样出现。
   插件的“回收站/已归档”只列它自己管的东西，自然也没有。
3. **为什么连删都删不掉（插件缺陷）**：插件 `checkSessionId()` 用
   `/^session-[A-Za-z0-9._-]+$/` 校验 id，而这三条会话是**旧格式 id**（没有 `session-` 前缀，
   日志头 `"id":"5c882a6f-…"`、目录名同），于是所有端点一律 `INVALID_SESSION_ID`：
   ```json
   {"ok":false,"error":{"code":"INVALID_SESSION_ID","message":"invalid session id: \"419c080d-…\". Expected \"session-…\"."}}
   ```
   实测三条 `POST /Session-Manager-GUI/trash/add` 全部被拒。

**修复**：放宽为 `/^(?:session-)?[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/`——
两种 id 形状都接受，同时仍然排除路径分隔符、前导点、纯 `..` 与超长 id（id 会作为路径段使用）。
错误文案改为 `Expected "session-…" or a legacy bare uuid.`。
判定用例：接受 3 条幽灵 id + 2 条现行 id；拒绝 `""` / `..` / `../etc/passwd` /
`session-../../x` / `/abs/path` / `a/b` / `-leading` / 200 字符超长。

**部署与生效**：已同步到 `~/.dsh/profiles/node_modules/Session-Manager-GUI/lib/index.js`
（与工作树逐文件同哈希 `ba46322e…`）。**主机半面不参与 HMR**：`dsh-base` 里
`cordis-plugin-hmr` 行是 `disabled: true`（模块热重载按 profile 选装，web profile 未开），
`patchReload: live` 只做补丁配置的监听——所以**必须重启 `dsh web`** 才生效；
重启前实测仍返回旧的 `INVALID_SESSION_ID`。

**重启后的删除方式**（三选一，详见《使用手册》§3.5）：
`POST /Session-Manager-GUI/trash/add {"sessionId"}` → 设置→会话回收站→“彻底删除”；
或直接 `POST …/trash/purge`；或由我代跑。

---

## 21. 0.1.6 缺陷修复与验收：粘性 `removed`、被冻结的 scope、以及一个会静默失效的初始化时序（2026-09-19）

> 本轮由新会话接手完成。0.1.6 的主体功能（级联删除、常规不可见会话页、路径守卫、僵尸
> Agent 修复）在上一会话已完成，交接时仅剩 1 个客户端缺陷；实际接手后发现是 **3 个缺陷**，
> 其中 2 个此前未被识别。

### 21.1 缺陷一：删除"当前打开"的会话 → 恢复后该标签页一直"会话不可用"

**现象**：在侧栏删除**当前正在打开**的会话 → 恢复 → 输入框显示"会话不可用"；切到别的会话
再切回来仍然是"会话不可用"；**F5 后一切正常**。对照：删除**没有打开**的会话 → 恢复 →
打开，一切正常。

**判定"是不是插件的锅"（三步证据链）**：

1. 上游 `dsh-api-session-controller/lib/client.js` 全文件只有**构造函数**里写
   `removed = false`（~1545），`handleRemoved()`（~1955）只置 `true`，
   `handleSessionAdded()`（~2706）**不会**复位它 ⇒ `removed` 是**粘性标记**；
2. `dsh-client-ui-conversation` 的输入框 `disabled = removed || inert || !live || …`
   （~15855），`disabled` 时占位文案即 `placeholder.unavailable`（"会话不可用"）；
3. `pruneScopes()`（~3428）会**跳过舞台上的会话**——`if (id === this.watched) { deferredRemovals.add(id); continue; }`
   ——留待"舞台移动"时清理。而恢复后 `eligible(id)` 重新为真，`sweepDeferred()`（~3465）
   只把该 id 从 `deferredRemovals` 删掉、**保留旧 scope record**。

⇒ 结论：恢复后那条旧 scope 仍然活着，其 `record.session` 就是带 `removed = true` 的旧实例，
视图继续经它渲染。**F5 之所以有效，是因为它重建了整个 store。**

**运行时实测（关键证据）**：往宿主注册了一个**只读**的动态 Cordis 客户端插件，在页面右下角
放一个面板读取活的 store 状态，复现后一键抓取，得到：

```json
{ "list": { "current": "session-<id>…", "phase": "ready" },
  "store": { "watched": "session-<id>…", "scopesSize": 2,
             "scopeIds": ["session-…", "session-<id>…"], "deferredSize": 1 },
  "currentBinding": { "present": true, "key": "session-<id>…",
                      "hooks": ["session","conversation","input","trajectory","chat"],
                      "services": ["conversation"] },
  "perSession": [{ "id": "session-<id>…", "scopePresent": true, "scopeRemoved": true,
                   "managerRemoved": true, "bindingRemoved": true,
                   "bindingSnapshotRemoved": true }] }
```

即：**scope 确实还在（scopePresent: true / scopeRemoved: true）、`deferredRemovals` 仍有 1 项**，
视图绑的就是那个 `removed` 实例。用户同时观察到"完整会话界面都在（历史、模型选择器、
发送按钮），只有输入框灰着"，与 `currentBinding` 有 hooks（`useSession` 走标准 hooks 源）
而 `ctx` 上只剩 `conversation`（`input`/`keyboard`/`inputActions` 已随 scope 失效）完全吻合。

### 21.2 缺陷二：恢复"未分类"目录下的会话报内部错误

**复现取到真实错误**：

```
POST /Session-Manager-GUI/trash/restore → HTTP 500
{"ok":false,"error":{"code":"INTERNAL","message":"ENOENT: no such file or directory, realpath '/home/<user>/<workspace>'"}}
```

`restoreOneEntry()` 走 `reg.resolveByPath(cwd)` → 上游 `dsh-workspace` 的
`realpathNormalize()` 调 `fs.realpath`，路径不存在即抛 ENOENT；异常冒泡到外层
`rollbackMove()`，把刚搬回 sessions 树的日志**又搬回回收站**，用户看到"内部错误"。
根因：**会话 header 里的 `cwd` 只是记忆**——目录可能已被移动或删除，而恢复是关于"日志"的，
不该被工作区分组拖累。

### 21.3 缺陷三（本轮新增功能）：回收站里看不出哪个是子代理

判定必须用会话 header 的 `origin === "subagent"`，**不能复用 `cascadeOf`**——后者只记录
"级联删除"这一种来源，单独删掉的子代理、或恢复后再次删除的子代理都没有它。
难点在于：软删除会把日志目录**移出** `$DSH_HOME/sessions`，而那正是
`sessionPersistence.list()` 唯一扫描的树 ⇒ 被删会话在 harness 服务里**查不到 header**，
只剩日志本身这一个权威来源。

### 21.4 本轮最重要的教训：客户端插件在 `apply()` 阶段拿不到 `sessions`，而且会静默失效

这是本轮**最值钱**的一条，也是前四轮修复"看起来部署了、实际毫无效果"的真正原因。

插件原来的 `inject` 只声明了 `["slots", "locale"]`，于是在 `apply(ctx)` 里：

```js
clientSessions = ctx.get("sessions") || null;   // ← 此刻服务还没发布，得到 null
…
function reviveSessionInstances(sessionIds) {
  if (clientSessions == null || …) return;      // ← 每一次都在这里静默返回
```

`apply()` 确实执行了（它安装的菜单删除事件桥、三个设置页注册都正常工作，所以从表面看
"模块是活的"），但**这个 null 让修复函数永久空转，且不报任何错**。多轮修复因此全部落空。

**决定性证据**（一条日志定案）：

```
[SMG-DIAG] client-module-loaded { build: "0.1.6+reviveA2+retry",
  hasSessions: false, hasScopes: false, hasStartScopeDrop: false,
  hasClear: false, hasOpen: false, sessionsKeys: null }
```

**正确写法**（已落进代码并写了注释）：

1. `inject` 里**声明 `"sessions"`**——Cordis 会等该服务就绪再执行 `apply()`（服务被替换时
   还会重新加载插件）；
2. 句柄**惰性获取**（`sessionStore()`），调用时才从活的 context 取，彻底免疫时序变化。

修复后同一条日志变为 `hasSessions: true`（build `0.1.6+reviveA2+lazy`）。

**同类教训**：诊断日志里若出现 `ctx.get(X) || null` 这种写法，X 又是一个在 `apply` 之后
才发布的服务，就等于给自己埋了一个**永不报错的空转开关**。要么 `inject`，要么惰性取。

### 21.5 第二条教训：`clear()` + `open(id)` **不是**"舞台移动"

第一版修法（以及中途两版）试图复用产品自己的舞台移动路径：先 `clientSessions.clear()`、
再 `clientSessions.open(id)`，期望 `followCurrent()` 跑完 `sweepDeferred()` 与 `session.open()`。
**实测证明这条路走不通**：两者都用 `notifier.notifyNow()` **同步**发通知，于是
`followCurrent()` 在**同一个 tick 内被调用两次并两次早退**：

- 第一次：`current` 已抹空，但 `watched` 还是旧值 → `current === watched` 不成立却因
  `current === undefined` 早退，`watched` **没被更新**；
- 第二次：`current` 回到同一个 id，而 `watched` 还是那个 id → `current === watched` **再次早退**。

结果：**舞台从未移动**，`session.open()` 从未被调用，新 materialize 出来的实例停在
`cold`，历史窗口为空。用户看到的现象是"输入框能用了，但聊天历史没了、placeholder 不消失"。

实测数字（同一事件、两种写法）：

| 写法 | 结果 |
| --- | --- |
| 丢 scope + `clear()`/`open()`（舞台移动） | `bindingOpenState: "cold"`、`eventEntries: 0` |
| 丢 scope + **显式 `binding()` 材料化 + `session.open()`** | `bindingOpenState: "open"`、`eventEntries: 100` |

**最终修法**（`reviveSessionInstances()` + `ensureRevivedSessionsOpen()`）：

1. 丢弃被污染的 scope：`scopes.delete(id)` + `startScopeDrop(id, record)`（照抄产品
   `pruneScopes` 的做法：先离开 map，再走 `dropScope` 的 `unbindScope` + fiber dispose +
   `manager.drop`），并顺带清掉 `deferredRemovals` 里该 id 的延后项；
2. **显式材料化 scope**：公开 API `clientSessions.binding(id)` 会按需建新 scope 与新实例
   （`removed` 只在构造函数里初始化为 false ⇒ 新实例天然是活的）；
3. **显式 `session.open()`** 回填历史窗口（幂等：open 中或已 open 直接返回）；
4. 编排重试 `[0, 1200, 3000, 6000]ms`——客户端列表、store 投影与会话远端不在同一个 tick
   settle，单发一次实测会打空（同缺陷的轮询版修法能稳定修复，正说明差的是时机与次数）；
5. 只在"该会话正是舞台上那一个"时才动手：恢复一个**没打开**的会话（或级联子会话）绝不能
   把视图抢过去。已读 `openState === "open"` 即短路，避免无谓重渲染。

### 21.6 缺陷二的修法

先 `pathExists(cwd)` 再决定工作区分组：

- 目录还在 → 原逻辑不变（`resolveByPath` → 必要时 `create` → 必要时 `attachSession`）；
- 目录没了 → **照常恢复日志**、跳过工作区分组、落到"未分类"，并打一条带解释的警告；
  **绝不为一个 stat 不到的路径伪造 workspace 记录**——上游把 workspace 定义为"存在的目录"，
  留一条指向空处的持久记录会让之后每次路径解析都失败。响应新增 `workspace_accounted` 字段。

### 21.7 缺陷三的修法

- 主机新增 `trashedHeader(entry)`：从**回收站存放区的日志**读 header。按 zstd 魔数
  `28 B5 2F FD` 切帧、**只解第一帧**（header 在首行）；路径先过既有的 `assertSessionDir`
  守卫（条目被篡改成指向别处就放弃标记，绝不读任意文件）；
- 日志不可读时降级到投影检查点里的 `subagent` 投影（产品对非子代理写 `{}`，对子代理写带
  `identity.mode` 的对象）；两者都没有 → **保持不标记**（不猜、不抛错）；
- `GET /trash/list` 输出新增 `isSubagent` / `parentSessionId` / `subagentLabel` / `isCascaded`；
- 浏览器半面：行前加胶囊标签「子代理」。图标取自 DSH 内置图标库的
  `IconAgentPresetOutline16`（厂商自用的一节点分叉两节点的层级图元），SVG 原样内联
  （三圆 mask + 四条 path，`currentColor`），因为本客户端半边不经编译、也不能依赖
  primitives 包。

### 21.8 诊断探针的清理（本轮特有）

因为浏览器端的运行时状态在会话内不可见，本轮**临时**往 `lib/client.js` 埋了探针：
`diag()` / `diagSessionState()` / `diagLiveSession()` 三个辅助函数 + 9 处调用点
（`client-module-loaded` / `revive:start` / `revive:per-id` / `revive:after-catalog` /
`open:skip` / `open:no-session` / `open:already-whole` / `open:called`）。它们
**只往控制台打叶子字段日志、从不修改行为**，是定位 21.4 那条时序问题的唯一手段
（`hasSessions: false` 就是它报出来的）。

验收通过后已**全部移除**（`lib/client.js` 1699 → 1531 行，-168 行），
`node --check` 通过、自测 **112/112** 保持全绿。以后遇到需要看浏览器内活状态的场景，
可复用这个手法：宿主注册一个只读动态 Cordis 客户端插件 + 页面角落面板。

### 21.9 交付状态

| 项 | 值 |
| --- | --- |
| 版本 | **0.1.6**（本轮未再改动版本号） |
| `lib/index.js` | 2106 行，sha256 `1459c722a0dad0fc…` |
| `lib/client.js` | 1531 行，sha256 `4ea9ddb8547e652a39…` |
| `install/selftest.mjs` | 开发树 **124 passed / 0 failed / 7 skipped**；发布树 **131 passed / 0 failed**（见 §21.12） |
| 已安装副本 | `~/.dsh/profiles/node_modules/Session-Manager-GUI/`，与工作树逐文件一致 |
| 生效方式 | 主机半面（`lib/index.js`）**必须重启** `dsh web`；浏览器半面（`lib/client.js`）随 `/plugins/…&rev=<内容哈希>` 下发，rev 变化即缓存失效，新开标签页即可（关标签页重开**不必**硬刷新） |

### 21.10 本轮新增验收项（已并入设计草案 §4 的建议）

1. 删**当前**会话 → 恢复 → **不刷新、不切走**：输入框可用、历史自动回填（实测 1 秒多）、
   AI 正常回应、再 F5 后消息仍在；
2. 删**非当前**会话 → 恢复 → 打开：正常（回归）；
3. 恢复带子代理的会话：父子都可用；
4. 恢复 cwd 已消失的会话：**成功恢复日志**、落到"未分类"、不报内部错误、不伪造 workspace 记录；
5. 回收站：子代理条目**带标志**、顶层会话**不带**、日志损坏时**不标记也不报错**。

### 21.12 发布包制作与"发布卫生"守卫

**目录约定**（沿用 0.1.1 起的做法）：

| 产物 | 内容 |
| --- | --- |
| `Session-Manager-GUI-<版本>/` + 同名 `.zip` | 完整快照：插件本体 + `install/`（补丁脚本、样板、自测）+ 两份文档 |
| `Session-Manager-GUI-<版本>_Github/` + 同名 `.zip` | 发布版：在上面基础上加 `LICENSE`（The Unlicense）、根 `CHANGELOG.md`，文档按"仓库化"措辞改写（见下） |

zip 顶层目录即版本目录名，解压即得到可直接使用的仓库/工程目录。

**发布版对文档做的两类改写**：

1. **脱敏**——发布包会被公开，所以本机路径、账号名、以及**真实会话 id** 一律不得出现。
   本轮的替换：`$DSH_HOME` 取默认值（即 `~/.dsh`） → `$DSH_HOME` 取默认值（`~/.dsh`）；
   `session-<id>…` → `session-<id>…`；
   `realpath '/home/<user>/<workspace>'` → `realpath '/home/<user>/<workspace>'`；
   一个临时测试会话 → 一个临时测试会话；使用手册里的 npx 路径改为 `~/` 开头。
2. **仓库化**——开发日志头部改为仓库视角（版本 0.1.6、加章节索引、注明
   `session-editor` 不在本仓库内）；README 追加"许可"段。

**新增"发布卫生"自测组（`selftest.mjs` 第 21 组）**：把上面第 1 类改写变成**可执行断言**，
避免下次打包漏掉某处。要点：

- 用**精确串**而不是宽泛正则检查（`/home/<letters>` 会误伤测试脚手架里的
  合成路径 `/home/user/work`；真实会话 id 是 `session-<8位hex>-<4位hex>-…` 形状，可精确匹配）；
- **开发树保留本机真实路径**（文档把它们当证据引用），因此该组只在发布树上真正执行，
  开发树上报 `skip`。区分方式是一个**自带的哨兵文件** `install/.dev-tree`
  （只存在于开发树，打包时不复制）——**不要**用"同级有没有别的目录"来推断：
  本工程的发布目录恰好位于开发目录内部，按布局推断会误判（本轮实测踩过一次）。
- 曾经写过一条"证明本文件从不拼写账号名"的断言，**已删除**：检查本身即一次拼写，
  只会自己命中自己。该性质改为**结构性保证**（关键词在运行时由碎片拼装）+
  **发布前用外部 grep 复核**。

**自测条数的两种口径**（避免误读）：开发树上 `124 passed, 0 failed, 7 skipped`；
发布树上 `131 passed, 0 failed`（跳过的 7 条转为真正执行的断言，条数因此不同）。
两者都以 `0 failed` 为通过标准。

### 21.13 "文档还写着上一版版本号"这类错误，必须由断言来防

0.1.6 的发布包做好之后，用户翻手册时发现**头部仍写着"当前版本 0.1.5"**。
复查后发现同一版错在两处：使用手册头部与 §0 版本线表格、以及开发日志头部
（0.1.5 的发布包大概也带着同样的问题，因为 0.1.5 的头就是这么写的）。

这类错误的特点是**完全静默**：没有任何检查会失败、没有任何日志会打印，
只是发布出去是错的。所以本轮把它也变成断言（`selftest.mjs` 第 21 组）：

- 以 **`package.json` 的 `version` 为唯一真值**，要求**四处声明**与之逐字一致：
  使用手册头部、§0 关系表的"版本线"单元格、插件 README 头部、开发日志头部；
- 新增断言后立刻抓到开发日志头部仍是 `0.1.5` —— 证明这条断言是有效的，
  而不是写完就永远为真。

**注意区分**：这些断言检查的是"**当前版本**"声明，所以文档里对**历史版本**的
正当引用（`0.1.5` 的版本记录、`DSH 0.1.5-rc.2` 这个 Harness 版本号、`0.1.5 早期
版本的 checkSessionId()` 等）不受影响，也不应被"批量替换"误伤 —— 这也是为什么
断言只锚定"当前版本"这一种句式，而不是全局搜版本号。

**本轮发布产物**：`Session-Manager-GUI-0.1.6/`、`Session-Manager-GUI-0.1.6_Github/`
及两个同名 zip；解压后实测 `135 passed, 0 failed`，`patch-ui.js --status` 三目标
`patched: yes`，全树零泄漏、四处版本号声明一致。
