# 0.1.6

## 新功能

- **子代理会话随父会话一起删除、一起恢复**：父会话与它衍生出的子代理会话按"父子绑定"成对处理。
  删除父会话时，其子代理会话（含多级派生）一并移入回收站；恢复时一并恢复。删除前会先给出
  提示，注明本次将同步影响的子代理会话条数。派生关系（fork）**不会**被级联——只处理
  `origin === "subagent"` 的会话层级。
- **新增设置页「常规不可见会话」**：集中列出平时在侧栏看不到的会话——子代理会话（区分
  "父会话仍在"与"遗留")、空白会话、无归属会话，附统计行与运行中标记，支持按组批量彻底删除。
  有了它，`@` 里能搜到却哪都找不到的"幽灵会话"可以正常清理了。
- **回收站现在会标注子代理会话**：子代理条目在标题前带一个"子代理"标志（配 DSH 内置的
  层级图标），鼠标悬浮可看到它属于哪个父会话。此前回收站里无法区分普通会话与子代理会话。

## 修复

- **删除"当前正在打开"的会话后，恢复它不再需要刷新页面**。此前该标签页会一直显示
  "会话不可用"（输入框灰掉、切走切回也没用），只能按 F5 解决；现在恢复后输入框立即可用、
  聊天历史自动回填、AI 正常回应。
- **恢复会话不再因原工作区目录已不存在而失败**。此前若会话记录的工作区目录被移动或删除，
  恢复会报内部错误并且日志被退回回收站；现在会照常恢复日志，会话落到"未分类"，
  并在日志中说明原因。
- **修复"僵尸会话"导致后续消息石沉大海**：删除会话时若只卸载会话上下文而未同步退役其
  代理注册项，之后发给该会话的消息会进入队列却永不落盘、永无回应，且该 id 占位导致无法
  重新恢复。现在删除、级联删除、恢复、启动清扫四条路径都会成对退役代理注册项。
- 修正"运行中"判定：代理注册表的运行状态与会话存储自报的 running/busy 取并集，
  避免会话正在执行时被删除。

## 安全

- **新增路径白名单守卫（两层）**：所有文件操作（删除 / 移动 / 创建 / 写入）只允许作用于
  Harness 会话数据范围内的四类路径——`$DSH_HOME/sessions/**`、回收站存放区、
  会话投影缓存、回收站索引；第二层用 `realpath` 解析真实路径，阻断"符号链接父目录逃逸"。
  即使回收站索引被篡改，也无法把删除指向工作区里的交付物。**插件绝不删除或写入任何
  工作区文件。**

## 内部

- 入口 API 新增 `GET /subagents/children`（删除前只读预览子代理条数 / 标题 / 运行中 /
  是否在回收站）、`GET /hidden/list`、`POST /hidden/purge`（批量彻底删除，校验 id 当前确实
  位于不可见集合，防陈旧页面误删）。
- 自测从 97 条扩充到 **112 条**离线断言，全部通过；新增覆盖级联闭包边界、路径守卫、
  恢复时的目录缺失分支、回收站子代理标注等。
- 兼容 DeepSeek Harness `0.1.5-rc.2` web profile。
